# Copy to .env and fill in (never commit .env)
TELEGRAM_BOT_TOKEN=
# Your numeric id from @userinfobot
TELEGRAM_ALLOWED_USER_IDS=
OPENROUTER_API_KEY=

# Опционально для docker compose локально: скопируй в .env (на Jenkins не используется).
