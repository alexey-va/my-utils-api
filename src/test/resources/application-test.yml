spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myutils
    username: myutils
    password: myutils
  data:
    redis:
      host: localhost
      port: 6379
  temporal:
    connection:
      enable: true
    test-server:
      enabled: true
    workers-auto-discovery:
      packages:
        - dev.myutils.api.temporal

myutils:
  temporal:
    enabled: true
